{
  "name": "landing-pages-armazem-diesel",
  "version": "1.0.0",
  "private": true,
  "description": "Landing pages estáticas da Armazém Auto Peças + Vercel Function para consulta de veículo.",
  "scripts": {
    "dev": "next dev",
    "build": "node scripts/validate-configs.mjs && next build && node scripts/verify-build.mjs",
    "start": "next start",
    "verify": "node scripts/verify-build.mjs",
    "validate-configs": "node scripts/validate-configs.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3",
    "next": "^16.2.4",
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "vitest": "^4.1.5"
  }
}
