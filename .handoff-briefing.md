# Handoff briefing — Sprint LPs Injeção Diesel (Armazém Auto Peças)

> Cole este arquivo INTEIRO no início de uma nova sessão de IA pra dar contexto.
> Atualizado: **2026-04-29 (tarde)**.

## ⚠️ Protocolo obrigatório de sessão (pacto Diego 2026-04-29)

**Ao ABRIR uma sessão de LP** (Diego mencionou LP/Amarok/HR/Bongo/etc OU abriu o repo):
1. Leia este briefing INTEIRO
2. Leia o último `RELATORIO_YYYY-MM-DD*.md` em `/root/armazem-projects/landing-pages/`
3. Rode `git status` + `git log --oneline -5` no repo
4. Resuma em <100 palavras o que foi feito + onde paramos + decisão pendente
5. Pergunte o que ele vai atacar hoje

**Ao ENCERRAR sessão** (Diego disse "vou sair" / "tô indo" / "encerra" / "manda o relatório" / similar):
1. Atualize ESTE arquivo (`.handoff-briefing.md`) com o estado novo
2. Escreva `/root/armazem-projects/landing-pages/RELATORIO_YYYY-MM-DD.md` (ou `_TARDE.md`) com TL;DR + decisões + commits + arquivos mexidos + próximo passo + como retomar
3. Commit do briefing atualizado (sem push)
4. Salve memory novas se aprendeu algo não-óbvio
5. Confirme em mensagem curta: artefatos + branch + próximo item

**Detalhes completos do protocolo:** `~/.claude/projects/-root/memory/feedback_diego_protocolo_sessao_lps.md`

## Quem é você (IA)

Você é o assistente do Diego (funcionário Armazém Auto Peças, conduz a sprint de LPs). Diego tem TDAH, é executor, prefere 1 etapa por vez, energia alta. Vinicius é o dono — não é ele que tá te falando, é o Diego.

## Quando ele avisar que vai sair

Escreva `RELATORIO_YYYY-MM-DD.md` (ou `_TARDE.md` se já tiver um do dia) em `/root/armazem-projects/landing-pages/` antes de fechar a resposta. Padrão de conteúdo: TL;DR, decisões, commits, arquivos mexidos, próximo passo, fila de LPs, como retomar.

## Stack e diretórios

- **Repo**: `/root/armazem-projects/landing-pages/landing-pages-armazem-diesel/`
- **Stack**: Next.js 16.2 SSG + React 19 + Vercel + Upstash Redis (rate limit/cache na API `consulta-veiculo`)
- **Deploy**: Vercel (projeto na conta pessoal do Diego — transfer pro team Armazém pendente do Vinicius)
- **Repo GitHub**: `git@github.com:viniciusvdalpra/landing-pages-armazem-diesel.git`
- **Prod URL**: `https://landing-pages-armazem-diesel.vercel.app/<slug>/`
- **Bunny Edge Rule (Sidnei)**: depois pra ficar `armazemautopecas.com.br/injecao-diesel/{slug}/`

## LPs em produção (PSI mobile / desktop)

| LP | Mobile | Desktop |
|---|---|---|
| `app/amarok-bico-injetor/` | 96 | 100 |
| `app/hr-bico-injetor/` | 92 | 99 |

## Roadmap de LPs (em ordem)

🔜 Kia Bongo III · ⏳ Kia K2500 · ⏳ Kia K2700 · ⏳ Toyota Hilux · ⏳ Ford Ranger · ⏳ Chevrolet S10

## Como Diego trabalha

- "Atualizar/commitar" = `git add` + `git commit` (NUNCA push — só `@devops` ou Diego puxa o gatilho)
- "Bora bora" / "vai" = autorização pra executar
- Pede sugestão de skill `lp-armazem` quando vai gerar LP nova
- Não quer palavrão nas respostas
- Aprecia organização visual em `/root/`

## Memory crítica pra essa sprint

(Salvas em `/root/.claude/projects/-root/memory/` — leia se relevante)

- `feedback_lp_amarok_v6_ou_tdi.md` — Amarok = só 2 bicos (V6 ou TDI 2.0). Cliente não escolhe cv.
- `feedback_lp_amarok_pixel_404.md` — pixel da Armazém retorna 404 nos 2 LPs (não foi instalado)
- `feedback_lp_preload_font_pode_degradar.md` — preload de font crítica DEGRADOU mobile, não aplicar cego
- `feedback_lp_validar_oem_componente_vs_completo.md` — validar OEM peça completa vs componente
- `decision_lp_fabricante_dinamico.md` — LPs multi-marca usam termo genérico no SSR + badge real no card
- `feedback_diego_resumo_ao_sair.md` — escrever RELATORIO_YYYY-MM-DD.md ao sair

## Skill `lp-armazem`

Em `~/.claude/skills/lp-armazem/SKILL.md`. Cria LP nova clonando scaffold da Amarok. **Aviso:** a skill perpetua os tech debts #3, #4, #6, #8 — vai precisar atualizar depois que esses itens forem fechados.

## Auditoria 2026-04-29 — 19 achados, 6 fechados, 12 abertos

Detalhe completo em: `/root/armazem-projects/landing-pages/RELATORIO_2026-04-29_TARDE.md`

### Branch ativa
`fix/amarok-oems-refactor` — 6 commits ahead of main, **NÃO pushada**:
- `65cbf5e` fix(amarok): preencher OEMs reais + simplificar parts.js (V6 ou TDI)
- `552c323` fix(api): adicionar timeout de 5s no fetch do webhook N8N
- `566a1ff` feat(sitemap): gerar sitemap dinâmico via app/sitemap.js
- `b73957c` refactor(fonts): mover 12 woff2 pra public/fonts/ único
- `4688e82` docs: handoff briefing pra novas sessões de IA
- `ddbaf48` docs(handoff): adicionar protocolo de abertura/encerramento

### Próximo item técnico (se Diego topar)

**#3 — Componentes shared** (`app/_components/` ou pasta na raiz)

10 componentes byte-idênticos entre as 2 LPs hoje:
`Hero.jsx`, `TrustBar.jsx`, `SearchSection.jsx`, `FAQ.jsx`, `Testimonials.jsx`, `Selector.jsx`, `WhySection.jsx`, `FinalCTA.jsx`, `Footer.jsx`, `atoms.jsx` + `LandingClient.jsx` + `lib/parts.js` + `lib/wa.js` + `styles.css`

**Sensível:** `parts.js` faz `import CFG from '../config.json'`. Após mover pra shared, precisa receber CFG (parâmetro, context, ou factory). Diego decide.

### Outros itens abertos (ordem sugerida)
- #4 JSON-LD via config (helper que gera schema)
- #11 next/image (ganho marginal)
- #9 Schema validado pro config.json
- #18 Atualizar skill `lp-armazem` (depois de #3 e #4)
- #5 1 teste de resolvePart (depois de #3)
- #12 Hardening next.config.mjs
- #13 página raiz arbitrária (depende do Bunny rule do Sidnei)
- #14 lp-amarok/ legacy → archive

## Pendências externas

- **Sidnei**: Bunny Edge Rule
- **Vinicius**: transferência Vercel pro team Armazém ($20/mês Pro)
- **Diego**: subir foto real do Bosch HR
